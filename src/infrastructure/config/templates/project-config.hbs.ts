/**
 * @deprecated The Handlebars YAML template has been replaced by the programmatic
 * JSON generator in src/infrastructure/config/generator.ts (generateConfigJson).
 * This file is retained as an empty export to avoid import errors during migration.
 */
export default '';
